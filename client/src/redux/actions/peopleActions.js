export const allPeople = (people) => {
  return {
    type: 'ALL_PEOPLE',
    payload: people,
  };
};

export const allHedgehogs = (hedgehogs) => {
  return {
    type: 'ALL_HEDGEHOGS',
    payload: hedgehogs,
  };
};

export const allBeavers = (beavers) => {
  return {
    type: 'ALL_HEDGEHOGS',
    payload: beavers,
  };
};
